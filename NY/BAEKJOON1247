import sys
for i in range(3):
    N = int(sys.stdin.readline())
    sum = 0
    for _ in range(N):
        a = int(sys.stdin.readline())
        sum += a
    if sum > 0: print('+')
    elif sum == 0: print('0')
    else: print('-')
